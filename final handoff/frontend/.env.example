# Aventum frontend configuration.
# Copy to `.env.local` (gitignored) and adjust if your API is not on the default port.

# Base URL of the Aventum API. No trailing slash.
# The frontend NEVER talks to the database directly — this is the only backend it knows.
VITE_AVENTUM_API_URL=http://localhost:8000
